import type { NextConfig } from 'next';
const config: NextConfig = { serverExternalPackages: ['steel-sdk', 'playwright-core'], allowedDevOrigins:['100.121.141.15','oracle.lemming-vector.ts.net'] };
export default config;
